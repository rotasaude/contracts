# types — contrato de tipos da API (Ruby ↔ TS)

> **SCAFFOLD — não materializado.** Requer extração do contrato real da API (shapes de
> request/response dos controllers/serializers do `api`). Tarefa pendente da Etapa 10:
> a API ainda está estabilizando (admin endpoints, setup, gov.br). Extrair quando o
> contrato de API estiver estável, e versionar por `types-vX.Y.Z` (ADR 0015).
